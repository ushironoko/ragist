{
  "mcpServers": {
    "gistdex": {
      "command": "npx",
      "args": ["--yes", "@ushironoko/gistdex", "--mcp"],
      "env": {
        "NODE_NO_WARNINGS": "1"
      },
      "description": "Gistdex MCP Server (using npx) - Works the same as npx, directly executing the package with --mcp flag."
    }
  }
}

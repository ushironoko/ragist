{
  "mcpServers": {
    "gistdex": {
      "command": "npx",
      "args": [
        "--yes",
        "--loglevel=error",
        "@ushironoko/gistdex@latest",
        "--mcp"
      ],
      "description": "Gistdex MCP Server - RAG search system for Gist, GitHub, and local files. The database will be created in the current working directory. Specify 'cwd' field to set custom database location."
    },
    "gistdex-local": {
      "command": "node",
      "args": ["dist/cli/index.js", "--mcp"],
      "cwd": ".",
      "description": "Local development test - runs built version from project directory"
    }
  }
}
